<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Weather</title>
    <style>
        main {
            width: 100vw;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 20px;
        }

        h1 {
            font-size: 3rem;
        }

        a {
            padding: 20px;
            border: 2px solid #ccc;
            border-radius: 10px;
            text-decoration: none;
            color: white;
            text-align: center;
            width: 200px;
            font-size: 1.5rem;
            background-color: darkgreen;
        }

        a:hover {
            background-color: rgba(0, 100, 0, 0.8);
        }

        .weather-info {
            font-size: 1.2rem;
            text-align: left;
            width: 80%;
            max-width: 500px;
            border: 1px solid #ccc;
            border-radius: 10px;
            padding: 20px;
            background-color: #f4f4f4;
        }

        .weather-info p {
            margin: 10px 0;
        }
    </style>
</head>
<body>
<main>
    <h1>Погода для {{ weather.name }}</h1>
    <div class="weather-info">
        <p><strong>Температура:</strong> {{ weather.main.temp }}°C</p>
        <p><strong>Відчувається як:</strong> {{ weather.main.feels_like }}°C</p>
        <p><strong>Вологість:</strong> {{ weather.main.humidity }}%</p>
        <p><strong>Тиск:</strong> {{ weather.main.pressure }} гПа</p>
        <p><strong>Вітер:</strong> {{ weather.wind.speed }} м/с, напрямок {{ weather.wind.deg }}°</p>
        <p><strong>Координати:</strong> широта {{ weather.coord.lat }}, довгота {{ weather.coord.lon }}</p>
    </div>
    <a href="/weather">Назад</a>
</main>
</body>
</html>
